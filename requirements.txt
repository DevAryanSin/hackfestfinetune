# ============================================================
# Unified Requirements (all modules)
# Covers: api/, brd_module/, noise_filter_module/, integration_module/
# ============================================================

# --- API Runtime ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
gunicorn>=21.2.0
pydantic[email]>=2.8.0
python-multipart>=0.0.9
python-dotenv>=1.0.1

# --- Storage / Data ---
psycopg2-binary>=2.9.9
pandas>=2.2.2
requests>=2.32.3

# --- LLM / BRD / Export ---
groq>=0.31.1
markdown>=3.6
python-docx>=1.1.2
weasyprint>=62.3

# --- Integrations (Gmail / Slack / PDF) ---
google-api-python-client>=2.151.0
google-auth>=2.35.0
google-auth-oauthlib>=1.2.1
slack-sdk>=3.33.1
pymupdf>=1.24.12
